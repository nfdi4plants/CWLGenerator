import { Loader, LoadingOptions } from '../internal';
export declare class _ExpressionLoader implements Loader {
    load(doc: any, baseuri: string, loadingOptions: LoadingOptions, docRoot?: string): Promise<any>;
}
